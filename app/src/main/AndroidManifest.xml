<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abigail.clubconnecthomepage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Club_Instruction" />
        <activity android:name=".NewClubActivity" />
        <activity android:name=".Student_Connect" />
        <activity
            android:name=".Student_Search"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Edit_My_Club" />
        <activity android:name=".Instructions_Menu" />
        <activity android:name=".Main_Page" />
        <activity android:name=".Club_Favorites" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ResetPasswordActivity"
            android:label="@string/title_activity_reset_password"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignupActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>